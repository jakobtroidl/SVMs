function y = perc(w, X)
% Simulates a perceptron.
% The first argument is the weight vectorwand the secondargument is a matrix with input vectors in its columnsX.  The outputyis a binaryvector with class labels 1 or -1.   


end