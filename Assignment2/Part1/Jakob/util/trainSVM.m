function [alpha, w0] = trainSVM(X, t)
% Implementthefunction[alpha, w0] = trainSVM(X,t) using the quadratic optimizer (e.g., quadprog) 
% to solve the dual optimization problem. The vector alpha holds the optimal dual parameter values, 
% i.e., the lagrange multipliers ?i for all N input vectors 


end

